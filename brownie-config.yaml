# exclude SafeMath when calculating test coverage
# https://eth-brownie.readthedocs.io/en/v1.10.3/config.html#exclude_paths
reports:
  exclude_contracts:
    - SafeMath

network:
    networks:
        ropsten:
            host: https://mainnet.infura.io/v3/8bf1651500204e3cb32fe2b2a16121e1